function string(stringa) {

    try {
        if (stringa === 0) {
        }

        else if(stringa === 5){

        }

        return stringa
    } catch (error) {
        
        console.log('Errore,non è una stringa')
    }
    
}

const result = string()

console.log(result)